import excuse;

class IfDeletion {

    public static void main(String[] args) {
        boolean i;
        boolean j;
        int a;

        i = true;
        j = false;

        if(j){
            a = 10;
        }
        else{
            a = 0;
        }

        if(a < 1){
            a = a + 5;
        }
        else{
            a = a + 20;
        }

        if(i && !j && !(a < 5) && a + 10 < 30 && a * 0 < 1){
            excuse.dosome(a);
        }
        else{
            excuse.dosome(a + 2);
        }
    }
}